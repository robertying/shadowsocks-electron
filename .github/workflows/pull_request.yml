name: pull_request

on: [pull_request]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Install dependencies
        run: yarn
      - name: Run eslint
        run: yarn lint
      - name: Run typescript
        run: yarn typecheck
      - name: Build
        run: yarn build
